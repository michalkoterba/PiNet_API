# PiNet API - Environment Configuration Template
# Copy this file to .env and fill in your values
# Command: cp .env.example .env

# =============================================================================
# API Security
# =============================================================================

# API Key for authentication (REQUIRED)
# This key must be provided in the X-API-Key header for all protected endpoints
# Generate a secure random key using one of these methods:
#   - Python: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
#   - OpenSSL: openssl rand -base64 32
#   - Online: https://randomkeygen.com/
API_KEY=your_secure_api_key_here

# =============================================================================
# Server Configuration
# =============================================================================

# Port number for the API server (default: 5000)
# The service will listen on all network interfaces (0.0.0.0)
API_PORT=5000